# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @search-mind/backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t search-mind-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system search-mind-backend && \
          adduser --system -G search-mind-backend search-mind-backend

COPY apps/backend/dist search-mind-backend/
COPY apps/backend/package.json search-mind-backend/
RUN chown -R search-mind-backend:search-mind-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix search-mind-backend --omit=dev -f install

CMD [ "node", "search-mind-backend" ]
